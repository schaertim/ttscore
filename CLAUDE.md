# Instructions for Claude

- Do not visually verify UI/frontend changes (no starting dev servers to click through the app, no browser screenshots, no preview tool verification workflow). The user does this verification themselves. Type-checking and compiling is still fine/expected.
