pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "backend"

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
